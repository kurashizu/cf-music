name: Debug Secret
on:
  workflow_dispatch: {}
jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Check secret
        env:
          SECRET: ${{ secrets.IMPORT_WEBHOOK_SECRET }}
        run: |
          echo "Length: ${#SECRET}"
          echo -n "$SECRET" | sha256sum
